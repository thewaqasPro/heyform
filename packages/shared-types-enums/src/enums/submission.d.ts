export declare enum SubmissionCategoryEnum {
  INBOX = 'inbox',
  SPAM = 'spam',
  STARRED = 'starred',
  ARCHIVE = 'archive'
}
export declare enum SubmissionStatusEnum {
  PUBLIC = 1,
  PRIVATE = 2,
  DELETED = 3
}
